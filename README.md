# ad-loader-bot-detection
A lightweight ad-loader with passive bot detection (WebGL checks, OffscreenCanvas tests, asset loading probes, timing analysis) and IP-based geo filtering. Fully client-side, no user interaction required.
